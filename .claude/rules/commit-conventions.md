# Commit conventions

**Always apply this rule**

Git commit style — small, frequent commits with one-line messages

## Guidelines

- Make **small, frequent commits** for changes (one logical change per commit).
- Use **one-line commit messages**; keep them short and clear.
- Prefer concrete messages (e.g. "Add README for Cursor rules") over vague ones (e.g. "Update stuff").
